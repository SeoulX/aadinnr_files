apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-staging
  namespace: demo-apps
  labels:
    app: demo-app-staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-app-staging
  template:
    metadata:
      labels:
        app: demo-app-staging
    spec:
      containers:
        - name: demo-app
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
